import React from 'react';

const Header = (props) => {
  return (
    <div>
      <h1>Quaker -- All hail the Earthquakes</h1>
      <p style={{'textAlign':'center'}}>Click on either one of the buttons below to get information about
        earthquakes happening around the world..!</p>
    </div>
  );
}

export default Header;
